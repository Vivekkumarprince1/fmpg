<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="/css/management.css">
</head>
<body>
    <div class="form">
    <h1>Edit User</h1>
    <form class="add" action="/admin/users/edit/<%= user._id %>" method="POST">
        <label for="username">Username:</label>
        <input class="input" type="text" id="username" name="username" value="<%= user.username %>" required><br>
        <label for="email">Email:</label>
        <input class="input" type="email" id="email" name="email" value="<%= user.email %>" required><br>
        <label for="mobile">Mobile:</label>
        <input class="input" type="text" id="mobile" name="mobile" value="<%= user.mobile %>" required><br>
        <label for="role">Role:</label>
<select class="input" id="role" name="role" required>

    <option value="superadmin" <%= user.role === "superadmin" ? "selected" : "" %>>SuperAdmin</option>
    <option value="admin" <%= user.role === "admin" ? "selected" : "" %>>Admin</option>
    <option value="user" <%= user.role === "user" ? "selected" : "" %>>User</option>
    <option value="owner" <%= user.role === "owner" ? "selected" : "" %>>Owner</option>

</select>

        <button class="submit" type="submit">Save Changes</button>
    </form>
</div>
</body>
</html>
